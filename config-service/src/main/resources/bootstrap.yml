server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MicroservicesShopping/config-service.git
          repos:
            service-product:
              uri: https://github.com/MicroservicesShopping/service-product.git
              searchPath: config-data
              username: ${GIT_USER}
              password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t